version: "3.1"
rules:
  - rule: Greet the user
    steps:
      - intent: greet
      - action: utter_greet

  - rule: Goodbye the user
    steps:
      - intent: goodbye
      - action: utter_goodbye

  - rule: Default fallback
    steps:
      - intent: nlu_fallback
      - action: utter_default
